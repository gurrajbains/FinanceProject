// need to autosubmit the file input once the file has been selected 
//fix the fact that the AI predict button is just a garbage button that does nothing right now and make it look nice and have it actually call the backend and display the result once the backend is implemented
//fix the filter and sort buttons to be proper 
//clean up the code fix the graphic settings and fix the tol bars button to scale higher and properly. Add a market feature